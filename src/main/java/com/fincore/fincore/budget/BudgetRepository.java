package com.fincore.fincore.budget; import java.time.LocalDate; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface BudgetRepository extends JpaRepository<Budget,Long>{List<Budget> findAllByUserIdOrderByMonthDesc(Long uid);Optional<Budget> findByIdAndUserId(Long id,Long uid);Optional<Budget> findByUserIdAndCategoryIgnoreCaseAndMonth(Long uid,String category,LocalDate month);}
